import 'package:flutter/material.dart';

/// Night City palette. Every color in the app comes from here.
abstract final class CyberColors {
  // Backgrounds
  static const bg0 = Color(0xFF07070C);
  static const bg1 = Color(0xFF0E0F1A);
  static const bg2 = Color(0xFF141628);
  static const grid = Color(0xFF1B1E33);

  // Neon accents
  static const cyan = Color(0xFF00F0FF);
  static const yellow = Color(0xFFFCEE0A);
  static const magenta = Color(0xFFFF2A6D);
  static const violet = Color(0xFF7A5CFF);

  // Text
  static const text0 = Color(0xFFE6F1FF);
  static const text1 = Color(0xFF8A93B2);
  static const text2 = Color(0xFF4B5273);
}
